> 2026-09-26: 開発の正本は `C:\Users\tians\Bioconnect` へ移行しました。次回は同フォルダの README.md を読んでください。以下は移行前の記録です。

# Bioconnect 開発方針

更新: 2026-09-23

## 別会話からの再開方法

このファイルは会話の要約ではなく、Bioconnectの作業契約です。再開時はリポジトリ `C:\Users\tians\mini-app-platform` を開き、このファイル、`PROGRESS.md`、`git status` を順に確認してください。別のChatGPT会話やChatGPTプロジェクトの `sources/` は最新のコードや会話履歴を保証しません。

別会話へ貼る開始文:

> `C:\Users\tians\mini-app-platform` のBioconnectを継続してください。まず `apps/ecosystem-puzzle/DEVELOPMENT.md`、`PROGRESS.md`、`git status`、主要コードを読み、未完了項目から再開してください。Web版を先行し、食物連鎖・爽快感を優先します。ローカル実装と検証は進めてよいですが、公開・push・広告課金の有効化はしないでください。

## ユーザーが承認した方針
Web版を先に育てる。一つの盤面で食物連鎖と爽快な連鎖を楽しみ、象への到達とハイスコアを目指す。ローカルでの仕様調整・実装・テストは毎回の確認なしで進めてよい。食物連鎖と爽快感に沿うなら空腹・餓死・枯れを変更・削除してよい。公開、費用発生、広告・購入の有効化は別途確認。コミット・pushは明示依頼があるまで行わない。

## 作業サイクル
1. PROGRESS.mdとこのファイルを読んで未完了項目を確認。
2. 現在の遊びの問題を一つ選び、仮説と変更を小さくまとめる。
3. 実装、ルールテスト、複数速度のシミュレーション、ブラウザ試遊を行う。
4. 実測したことと仮説を分けて記録。到達可能というだけで楽しいと断定しない。
5. 節目に試遊版を渡し、ユーザーの感想を次の判断に反映。

この文書は継続時の作業方針。定期実行やバックグラウンド自動実行は設定していない。

## 優先順と完了条件
- [x] 配置の補充待ちをなくす。
- [x] 既存タイル側に合体結果を残す。左右対称時は上→左の固定順。配置前に合体数を表示。
- [ ] 連鎖の経路と最終着地点をスマホでも直感的に示す。現状の予告は連鎖数、着地点は読み上げとツールチップのみ。
- [ ] 合体演出を段階的に見せ、入力を邪魔せず因果が伝わるか検証。
- [ ] 食事と合体の両方を狙う意味を調整。現状は速いbotで合体点が約77%となり、餓死も多い。世話が罰になりすぎない設計を試す。
- [x] 象までの進行を試作。合体の進行順と食べる対象は独立したデータにした。中間動物は deer / zebra / buffalo / bear / lion / tiger の仮置き。
- [ ] 到達演出、再挑戦、スマホの画面内レイアウト、ベスト保存を検証。
- [ ] 広告と買い切り広告非表示の設計。プレイ中の連鎖を中断しない案を優先。価格・事業者・認証/購入復元は未確定であり、実課金を有効化しない。

## スコアの読み方（2026-09-22 変更）

内部の点数（生点）は一切変えていない。変えたのは見せ方で、生点を固定スケールに写す。

    表示点 = SCORE_CAP * x / (1 + x)、x = (生点 / SCORE_PACE) ^ (SCORE_CURVE_PCT/100)

SCORE_CAP=10000、SCORE_PACE=300000、SCORE_CURVE_PCT=70。狙いは三つ。

- 上限10000には永久に届かない。x/(1+x) は必ず1未満なので、終わりのある目標ではなく地平線になる。
- 稼ぐほど伸びにくい。段の到達に必要な生点は 13,000 / 41,000 / 89,000 / 168,000 / 300,000 で、一段ごとに約2.5倍。盤面のルールは何も変えずに難易度曲線だけが立つ。
- 遅い人も速い人も同じ物差しに乗る。指数0.7が下側を持ち上げるので、最も下手なbot（4tickごと）でも中央値1,180で第2段に乗る。

段は6つ。Bare ground(0) / Sprouting(1000) / Meadow(2000) / Thicket(3000) / Woodland(4000) / Wilderness(5000)。
スコアバーの下に段名と次の段までの残りとゲージを出し、終了カードにも段を出す。ポップアップと実況の「+N」も表示点の増分に変えた。同じ食事でも終盤ほど数字が小さくなるのが見える。
ベストは生点で保存し、表示時に写す。表示スケールが変わったので RULES_VERSION は13に上げ、旧ベストは無効化される。

`node sim.js 300` の実測（表示点）:

| bot | p25/p50/p75 | max | Lv6到達 |
| --- | --- | --- | --- |
| casual | 3485/4502/5139 | 6264 | 29% |
| careful | 1672/2953/4604 | 5829 | 11% |
| careful, banking | 1342/2476/4336 | 5780 | 9% |
| 2tickごと | 4070/5239/5887 | 6571 | 56% |
| 3tickごと | 1541/2180/3347 | 6275 | 5% |
| 4tickごと | 951/1180/1534 | 4506 | 0% |

1800試合の最高が6571。9000には生点690万＝これまでの最高試合の約10倍が要る。sim.js のスコア列は表示点に切り替えた（生点を見ても段の差が読めないため）。

## 現在の検証
ルール版12。象までの一本ラダー、各段の食性、成長点、即時手札補充、予告、クマ/象到達後の継続をルールテストで固定。`node apps/ecosystem-puzzle/rules.test.js` は 234 passed / 0 failed。
ブラウザ試遊、スマホ実機、長めの自動試遊はこの区切りでは未確認。到達可能というだけで楽しいとは見なさない。全体テストには既知のCRLF/LF比較差21件が残る（前回472成功）。
ローカルプレビューは http://127.0.0.1:8766/apps/ecosystem-puzzle/ 。旧8765は応答不安定のため切替。

## 2026-09-21 クマまでの試作
ユーザーの指示で今回の範囲をオオカミの次（クマ）までに限定。デザインも委任済み。オオカミ2匹→クマ、草優先・次にウサギを食べる。空腹20/餓死27 tick、合体基本点1500。ルール版11。茶色のSVGと専用初登場カードを追加。68件のルールテストと180試合の自動試遊に成功。検証盤面の実ブラウザで4段連鎖→クマ、9000点と登場カードを確認。
自動試遊のクマ到達率は速いcasual 70%、careful 100%、2tick毎の操作3%、3/4tick毎0%（各30試合）。ゆっくり操作する場合の到達難易度は未解決。広告・象・公開は今回対象外。

## 2026-09-22 象までの一本ラダー試作
前回リミット到達で中断したため、切りのよい保存点として現状を残す。実装はクマ追加からさらに進み、deer / zebra / buffalo / lion / tiger / elephant を追加。ラダーは sprout→grass→rabbit→fox→deer→zebra→buffalo→wolf→bear→lion→tiger→elephant。食性はラダー進行とは別データで、象は草食。音/音楽トグルと `audio.js` も追加済み。

次に見ること:
- 実ブラウザで象まで到達できるか、画面が破綻しないか。
- ゆっくり操作でも進行できるか。クマ時点で遅い操作の到達率が低かった問題は未解決。
- 食事が罰になりすぎず、合体と世話の両方を狙う意味があるか。
- 連鎖の経路と着地点がスマホでも直感的に伝わるか。
- `audio.js` のボタン位置、音量、ブラウザ権限まわりが邪魔にならないか。

## 2026-09-23 象をハイリスク・ハイリターンに作り直した

象は「到達したら終わり」の置物だった（草食・eatAt33/starveAt45）。終盤の判断を作るため、次の三点に変えた。ルール版15。

**合成動物しか食べない。** タイルに `born` を足した。`'raised'` は合体（growFrom）が生んだものだけで、手札から置いたタイル・石・骨は全部 `'wild'`。象の食性に `needs: 'raised'` を付け、`pickMeal` と `inReach` は共通の `edible()` を通す。これでラダーの最後が「到達できるか」ではなく「何を残してあるか」の問いになる。虎2匹を象にした瞬間、その虎は餌ではなくなる。

**空腹が2.5倍速い。** `ELEPHANT_HUNGER_PCT = 250` を基準値 33/45 に掛けて eatAt 13 / starveAt 18。定数は素の `const NAME = <number>;` なので sim.js で振れる。

**捕食点が4倍。** `ELEPHANT_MEAL_PCT = 400` を `feedEveryone` で掛ける。MEAL_VALUE 自体は触っていない（獅子や虎にとっての鹿は3000のまま）。象が合成虎を食べると72000で、他の全捕食の最大値の4倍。得点は噛んだ瞬間だけで、生きているだけでは1点も入らない。

死亡は既存の `collectDeaths` がそのまま効く（骨を残す）。追加ペナルティや死後に草を生やす処理は入れていない。

表示は三つ足した。合体で生まれた動物に紫の点（虎を発見してから出る。それ以前は答えるべき問いがない）。実況に「refused（隣にいるのに食べない）」と「nothing to eat（そもそも無い）」の別の文。ゴール行は象が出ている間、盤上の合成動物の残数を出す。

### 実測

`node rules.test.js` は 269 passed / 0 failed。`node test/run.js` は 472 passed / 21 failed で、21件は既知のCRLF/LF差分のまま。

`node sim.js 300` のスコアは前回とほぼ同じ（casual 中央値 4499、前回 4502）。象は稼ぎ頭になっていない。

`node sim.js 200 --elephant` を新設した。象に到達した試合のうち、象が一度でも食べた割合：

| ELEPHANT_HUNGER_PCT | beginner | intermediate | best known | 象の餓死(best known) |
| --- | --- | --- | --- | --- |
| 200 | 0% | 12% | 13% | 19 |
| **250（採用）** | 0% | 24% | 25% | 122 |
| 300 | 40% | 35% | 41% | 178 |

空腹が速いほど象は「よく食べる」。遅いと合成動物が枯れる・他に食われるほうが先に来て、象は腹が減る前に餌を失う。ここは直感と逆だったので、数値ではなく仕組みとして覚えておく価値がある。

botは合成動物を象のために取っておく打ち方を知らないので、この表は下限であって上限ではない。人間が意図して残せば上がるはず。ただし「象に届いた試合の4分の3で象が一度も食べない」は厳しすぎる可能性があり、300へ上げる判断は実際の試遊を見てから。

### 次に見ること
- 実際に遊んで、象へ上げるタイミングを迷う場面が生まれているか。紫の点が「残しておく」判断に繋がるか。
- 紫の点が小さい画面で見えるか。虎発見前に出さない判断が正しいか。
- 拡張案（今回は入れていない）：象の餓死は骨1個では軽すぎるかもしれない。死んだ場所の周囲に草を撒く、あるいは骨を複数残すなど、大物の死に見合う後始末を検討する余地がある。

## ルール版13：手札に天井をつける（HAND_CEILING）

### 問題

進むほど配られる動物が上がっていき、発見が次の発見を安くしていた。`rollHand()` は上位発見 `top` に追従して `LADDER[top - 1]` を65%で配っていたので、虎まで行くとライオンが降ってきて象が勝手に完成する。`node sim.js 300` で casual botの象到達率が **57%**。スイカゲーム型の「最後の1個は滅多に出ない」形になっていなかった。

### 変更

手札の上限を `HAND_CEILING = 7`（オオカミ）に固定した。

```js
const high = Math.min(HAND_CEILING, top - 1);
return LADDER[Math.max(2, high - (Math.random() < 0.2 ? 1 : 0))];
```

クマ・ライオン・トラ・ゾウの4段は、配給ではなく合成でしか手に入らない。天井は素の `const NAME = <number>;` なので sim.js で振れる。

### 実測（casual bot、象到達率）

| HAND_CEILING | 象到達率 | lv6 |
| --- | --- | --- |
| 6（バッファロー） | 0% | 2% |
| **7（オオカミ／採用）** | 1% | 8% |
| 8（クマ） | 21% | 19% |
| 9（ライオン） | 60% | 38% |
| 11（天井なし＝変更前） | 57% | 31% |

8と9の間が崖になっている。オオカミ止めでも **オオカミ97% / クマ94%** は変わらないので、途中までの手応えは落ちていない。落ちたのは終盤の自動完成だけ。

`node rules.test.js` は 172 passed / 0 failed。以前の「late hand never skips beyond discovery」を100回ループで回していた箇所を、全発見段×2000回で天井を検査する3本に置き換えたので件数が減っている（269→172）。この検査は天井が壊れたら落ちる。

### 文言

「手札は発見とともに増える」はオオカミ以降は嘘になるので、クマ発見以降の初回メッセージと遊び方ガイドの手順6を書き換えた。数値は出さず「配られるのはオオカミまで」と動物名で言っている。

### 次に見ること
- 1%は低すぎないか。実際に遊んで「あと一歩」が続くなら8（クマ）への緩和も選択肢。

## 2026-09-26 クマが絵になり、動き出した

クマはこれまでSVGのシルエットだった。`concepts/animal-art-v1/bear-brave-parent-cub-v2.png`（親グマと子グマ）を `make-game-asset.py` で `img/bear-whole.png` に書き出し、オオカミと同じ1枚絵のリグを与えた。空腹顔は作っていないので `calm` と `hungry` は同じファイル、空腹は sag とメーターで読ませる。

### 子グマが潰れる問題

親子は横長なので、幅をタイルに合わせると全体が低くなる。実測で44pxのとき高さ29px（オオカミは35px）、子グマは15pxで、しかも親の胴体に重なって同じ黒さなので、子グマではなく親の脇腹の塊に見えた。`concepts/animal-art-v1/bear-size-check.png` がその実測。

子グマだけを切り離す案は採らなかった。子グマは親の塗りの上に重ねて描かれていて、切り抜くには塗りの中をなぞることになる。ウサギとキツネがパーツ分割なのは最初からパーツごとに描かれているからで、完成画を切るのとは別の話。

採ったのは**寄せる**方。リグは絵をタイルに収めない。タイルより広く描いて（`w` 47 / span 38）左へ押し出し（`ox` 5.2）、親のお尻はタイルの外に落とす。残るのは親の頭と肩、そして胸の白い模様を背にした子グマ。44pxでも子グマの顔と白い胸が生き残り、背丈は38pxでオオカミの36pxを上回った（ラダー順と一致する）。絵のファイルは切っていない。切れ目はキャンバスの端でしかないので、枠取りは数値2つでやり直せる。`bear-crop-check.png` が比較した3案、`bear-rig-preview.png` が採用値の再現。

### 動き

`bear-sway`、4.2秒、足元を軸に±0.9度、半拍で0.5px浮く。ウサギ（2.6秒・上下1.6px・落ち着かない）と対になる「どっしり」を狙った。回転を1度未満に抑えたのは、このタイルには2頭いて、大きく振ると子グマが親の胸の上を泳ぐため。`nth-child` で3頭までずらして、並んだクマが揃って揺れないようにしてある。`prefers-reduced-motion` で止まる。

### 実測
`node rules.test.js` は 172 passed / 0 failed（ルールは触っていない）。実ブラウザ（127.0.0.1:8766）で盤面にクマ3頭・オオカミ・キツネ・ウサギを置いて確認。描画、子グマの可読性、揺れの動作と位相ずれを目視と `getComputedStyle` の両方で確認した。

### 茶色のカードはそのまま残す（決定）

絵が茶色から黒（ツキノワグマ）に変わったので、初登場カード `.first--bear` の茶色（#a57550 / #fff6df）とSVGフォールバック `#artBear` の茶色は絵と一致しなくなった。ユーザーの判断で**どちらも変えない**。カードの茶色は動物の毛色ではなく森の暖かさを出す色として残す。絵と色を揃える案は検討したうえで却下しているので、蒸し返さない。

### 次に見ること
- スマホ実機で子グマが読めるか。検証はデスクトップ（タイル78px）と計算上の44px再現まで。
